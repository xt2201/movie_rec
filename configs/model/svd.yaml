# SVD Model Configuration (using Surprise library)
name: svd
type: traditional

# SVD parameters
n_factors: 100
n_epochs: 20
lr_all: 0.005
reg_all: 0.02
biased: true

# Random state
random_state: ${seed}
